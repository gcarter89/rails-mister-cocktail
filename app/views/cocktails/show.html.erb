<h1> <%= @cocktail.name %> </h1>

<ul>
<% @doses.each do |dose| %>
  <li> <%= dose.description %> <%= dose.ingredient.name %>
  <%= link_to "DELETE", dose_path(dose.id),
        method: :delete,
        data: {confirm: "Are you sure?"} %>

</li>
<% end  %>
</ul>

<p><%= link_to "ADD INGREDIENTS", new_cocktail_dose_path(@cocktail) %>
<p><%= link_to "HOME", cocktails_path %>
